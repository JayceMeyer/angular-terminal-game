import { LineFormatPipe } from './line-format.pipe';

describe('LineFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new LineFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
